{
  "name": "ts-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "nodemon --watch src --watch package.json -e ts,json -q -x 'rm -fr dist && echo \"\\n============\\nCOMPILING...\\n============\\n\\n\" && tsc --outDir dist || return 0 && node dist/worksheet.js'",
    "test": "echo 'no test suite defined' && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/tape": "^4.2.28",
    "nodemon": "latest",
    "tape": "^4.6.3",
    "typescript": "latest"
  }
}
